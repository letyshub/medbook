# Server Configuration
# Port for the backend API server
SERVER_PORT=3001

# Client Configuration
# Port for the frontend dev server (Vite)
CLIENT_PORT=5173

# API URL for client to connect to server
# Default: http://localhost:3001 (npm development)
# Docker: http://server:3001 (set automatically in docker-compose.yml)
VITE_API_URL=http://localhost:3001
